---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<section id={id} class={cn("py-16 sm:py-24 scroll-mt-16", className)}>
  <slot />
</section>